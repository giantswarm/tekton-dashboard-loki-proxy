name: tekton-dashboard-loki-proxy

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

image:
  registry: quay.io
  name: giantswarm/tekton-dashboard-loki-proxy
  tag: "[[ .Version ]]"
  pullPolicy: IfNotPresent

replicas: 1

lokiEndpoint: ""
orgID: "1"
since: "120h"
